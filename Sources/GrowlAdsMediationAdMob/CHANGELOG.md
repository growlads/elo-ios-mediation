# Changelog — GrowlAdsMediationAdMob

All notable changes to the AdMob adapter are documented here.
The runtime version (`AdMobAdapter.version`) follows
`<vendor-major>.<vendor-minor>.<vendor-patch>.<adapter-patch>`.

## 11.10.0.0 — 2026-04-28

- Initial release in `elo-ios-mediation`. Extracted from
  `elo-ios-sdk-source`. Behavior unchanged from the version that
  shipped in `growl-ios-sdk@0.0.1`.
